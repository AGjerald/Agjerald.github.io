// Example: Log a message to check if it's working
console.log('JavaScript is linked and running!');

// Example: Add a scroll event
window.addEventListener('scroll', () => {
  console.log('You are scrolling!');
});
